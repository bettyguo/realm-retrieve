# Lightweight dev-only dependency set. Use `pip install -e ".[dev]"` for the
# full set declared in pyproject.toml — this file pins versions for CI.
pytest==7.4.4
pytest-cov==4.1.0
pytest-xdist==3.5.0
black==26.5.1
isort==8.0.1
ruff==0.15.13
mypy==2.1.0
pre-commit==4.6.0
